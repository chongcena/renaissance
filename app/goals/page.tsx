'use client';
import Layout from '@/components/Layout';
import { useStore } from '@/components/store';
const scales = ['year', 'month', 'week', 'day'] as const;
export default function GoalsPage() { const { goals, branches, createGoal } = useStore(); return <Layout><section className='space-y-4'><h2 className='text-xl font-semibold'>Goals</h2><button onClick={() => createGoal({ title: 'New Goal', pillarId: branches[0]?.id ?? '', scale: 'week', status: 'active', currentAction: 'Define current action' })} className='rounded bg-neon px-3 py-2 text-bg'>Add Goal</button>{scales.map((scale)=><section key={scale} className='rounded-xl border border-neon/30 p-4'><h3 className='text-sm uppercase text-neonDim'>{scale} goals</h3><div className='mt-2 space-y-2'>{goals.filter((g)=>g.scale===scale).map((g)=>{const pillar=branches.find((b)=>b.id===g.pillarId);return <article key={g.id} className='rounded border border-neon/20 p-2 text-sm'><p className='font-medium'>{g.title}</p><p className='text-xs text-muted'>{g.status} • {pillar?.name ?? 'Unassigned'} • due {g.dueDate ?? 'n/a'}</p><p className='text-xs text-neonDim'>Current Action: {g.currentAction || 'none'}</p></article>;})}</div></section>)}</section></Layout>; }
